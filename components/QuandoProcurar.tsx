// Componente em desuso no template. Mantido como stub pra evitar quebra
// de imports até a Task #8 (sections on/off) e Task #11/extras (seção
// "Sobre" CRUD) substituírem definitivamente.
//
// No template original (advocacia), exibia uma lista hardcoded de
// "situações em que procurar a banca". Não faz sentido pro template
// genérico — cada cliente fala sobre as suas atuações pela seção
// "Áreas/Serviços" + "Sobre" customizadas no admin.

export default function QuandoProcurar() {
  return null;
}
